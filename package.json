{
    "name": "resume-pipeline-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd frontend && npm install",
        "build": "cd frontend && npm run build && mkdir -p ../dist && cp -r dist/* ../dist"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}